const CACHE='bunt-cakes-v4';
const ASSETS=['./','index.html','manifest.webmanifest','icon.svg','enhancements.js'];
self.addEventListener('install',e=>{self.skipWaiting();e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)))});
self.addEventListener('activate',e=>e.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))),self.clients.claim()])));
self.addEventListener('fetch',e=>{const req=e.request;if(req.url.includes('/api/')){e.respondWith(fetch(req));return}if(req.mode==='navigate'){e.respondWith(fetch(req).then(async r=>{let html=await r.text();if(!html.includes('enhancements.js'))html=html.replace('</body>','<script src="enhancements.js"></script></body>');const out=new Response(html,{status:r.status,statusText:r.statusText,headers:{'content-type':'text/html; charset=utf-8'}});const copy=out.clone();caches.open(CACHE).then(c=>c.put('index.html',copy));return out}).catch(async()=>{const cached=await caches.match('index.html');if(!cached)return cached;let html=await cached.text();if(!html.includes('enhancements.js'))html=html.replace('</body>','<script src="enhancements.js"></script></body>');return new Response(html,{headers:{'content-type':'text/html; charset=utf-8'}})}));return}e.respondWith(caches.match(req).then(r=>r||fetch(req).then(net=>{const copy=net.clone();caches.open(CACHE).then(c=>c.put(req,copy));return net}))) });