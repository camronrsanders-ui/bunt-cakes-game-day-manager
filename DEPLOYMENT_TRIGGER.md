# Deployment trigger

This file was added after connecting the repository to Vercel to verify that commits create a new deployment.

Verification preview: field rotation redesign after function-count cleanup and static icon routing.